version: '3.8'

services:
  neo4j:
    image: neo4j:latest
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - ./neo4j_docker/data:/data
      - ./neo4j_docker/logs:/logs
    environment:
      - NEO4J_AUTH=neo4j/osint_admin123
      - NEO4J_PLUGINS=["apoc"]
